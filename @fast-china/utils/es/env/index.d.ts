/**
 * 环境工具类
 */
export declare const envUtil: {
    /**
     * 是否为 Uni 环境
     * @returns
     */
    isUni(): boolean;
    /**
     * 是否为 Web 环境（浏览器环境）
     * @returns
     */
    isWeb(): boolean;
    /**
     * 是否为手机设备
     * @returns
     */
    isMobile(): boolean;
    /**
     * 是否为平板设备
     * @returns
     */
    isIpad(): boolean;
};
